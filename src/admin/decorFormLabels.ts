import type { Locale } from '../i18n/types'

export const decorFormLabels: Record<Locale, { name: string }> = {
  uz: { name: 'Bezak nomi' },
  ru: { name: 'Название декора' },
  en: { name: 'Decor name' },
}
