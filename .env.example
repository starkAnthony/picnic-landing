# Local .env (copy to .env) — never commit .env

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Admin uploads on Vercel (Supabase → Settings → API → service_role secret)
SUPABASE_SERVICE_ROLE_KEY=

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
